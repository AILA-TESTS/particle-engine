{
  "name": "@particle-engine/video",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@particle-engine/animation": "workspace:*",
    "@particle-engine/core": "workspace:*",
    "@particle-engine/renderer-canvas": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "tsup": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
